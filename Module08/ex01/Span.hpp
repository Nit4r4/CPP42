#ifndef SPAN_HPP
#define SPAN_HPP

# include <iostream>
# include <string>
# include <algorithm>

#endif

class Span {
	private:
		unsigned int	_N;
	public:
		Span();
		Span(unsigned int N);
		~Span();
		Span(const Span &cpy);
		Span &operator=(const Span &assign);

		addNumber() {
			if (N)
				throw 
		}
};

shortestSpan() {}
longestSpan() {}